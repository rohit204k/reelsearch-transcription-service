steps:
  - name: gcr.io/kaniko-project/executor:latest
    args:
      - --destination=us-central1-docker.pkg.dev/$PROJECT_ID/reelsearch/transcription-service:latest
      - --cache=true
      - --cache-ttl=24h

options:
  logging: CLOUD_LOGGING_ONLY
